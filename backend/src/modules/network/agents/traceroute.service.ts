import { Injectable } from '@nestjs/common';

@Injectable()
export class TracerouteAgentService {
  async execute(config: any): Promise<any> {
    // Implementation de l'agent traceroute
    throw new Error('Non implemente');
  }
}
